<!DOCTYPE html>
<html>
    <body>
        <h1>Filters Manager Test Case.</h1>
        <label>Velocity works well while Filters Manager is added as a property.</label>
        <label>In this case velocity is able to access the parent class of filtered ones.</label>
        <label>Class.class passed to this template through velocity context</label>
        <label>In this case velocity is able to create new instances from parent of the filtered classes even through Class.class.</label>
        
        org.apache.velocity.test.filtersmanager.Car
    </body>
</html>